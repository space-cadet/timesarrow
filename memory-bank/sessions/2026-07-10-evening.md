# Session: 2026-07-10 Evening

**Started**: -
**Focus Task**: T32
**Status**: 🔄 ACTIVE

## Work Done

Working on T32: audited the published numerics deployment against the corrected local Quarto render, clarified the remaining correction gate work, synchronized the corrected `_site` output to `space-cadet.github.io`, repaired missing dashboard asset publishing, fixed T20 Phase 3b figure paths, normalized numerics page timestamps, added a `Last Updated` column to the main numerics task table, and redeployed the refreshed site via deploy commits `92d05cc`, `21a496e`, and `efe6780`.

Follow-up: ran the `T31` Rust unit tests successfully using `rustup` toolchain `1.92.0-aarch64-apple-darwin`, confirming the Rust 2024-compatible gauge-invariance tests pass. Then ran short `T20d` rerun calibrations to estimate production cost: `L=16` at `30k` thermal + `20k` measurement sweeps took about `11.2 s` per β, `L=32` at `20k` thermal + `10k` measurement sweeps took about `44.8 s` per β, and the tiny `L=8` test completed successfully. A full local `L=8` fine-scan rerun also completed as `numerics/data/fss/t20-p3b-L8-3D-fine-20260710.json`. Based on those timings, the remaining larger fine-scan reruns are being handed off to Kimi rather than launched to completion in this session.
